node_modules/
.env
.env.local
.env.*.local
.vercel

# tapas-store (customer e-commerce app)
tapas-store/build/
tapas-store/.env
tapas-store/.env.local
tapas-store/.env.*.local

# Visual-regression harness output — regenerated on every run.
tests/visual/output/

# Receipt print station: discovered printer address (per machine)
printer_bridge/.receipt-printer

# Generated at build time from printer_bridge/ — see scripts/sync-print-station.js
public/print-station.py
public/print-station-logo.pbm
public/install-print-station.sh

# Supabase CLI scratch (version check cache, project link ref)
supabase/.temp/
