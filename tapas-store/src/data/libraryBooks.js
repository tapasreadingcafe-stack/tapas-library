// Library page is removed; this module remains as a stub so the
// TapasFigmaBlocks library blocks can still be imported by the editor
// without crashing the build. The blocks render no data.

export const LIBRARY_CATEGORIES = [];
export const LIBRARY_FEATURED = [];
export const LIBRARY_HOUSE_RULES = [];
export function matchesBook() { return true; }
